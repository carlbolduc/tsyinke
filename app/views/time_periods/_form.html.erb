<%= form_for(@time_period) do |f| %>

  <%= f.label :start %>
  <%= f.date_select :start %>

  <%= f.label :duration %>
  <%= f.number_field :duration %>

  <%= f.check_box :locked %>
  <%= f.label :locked %>

  <%= f.submit yield(:button_text) %>
<% end %>