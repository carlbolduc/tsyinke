<% provide(:title, t('.title')) %>
<% provide(:button_text, t('button.update')) %>
<h1><%= t '.title' %></h1>

<%= form_for(@project) do |f| %>
  <%= render 'form' %>
<% end %>
<%= link_to t('link.cancel'), projects_path %>