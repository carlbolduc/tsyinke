var te = htmlToElement("<%= escape_javascript(render partial: 'time_entry', locals: { time_entry: @time_entry } ) %>");
new_time_entry_form = document.getElementById('new_time_entry');
new_time_entry_form.parentNode.removeChild(new_time_entry_form);
document.getElementById('time_entries').appendChild(te);